import tkinter as tk

def show_inconsistencias():
    root = tk.Toplevel()
    root.title("Inconsistências")
    
    tk.Label(root, text="Inconsistências", font=("Arial", 16, "bold")).pack(pady=10)
    # Adicione aqui os componentes e lógica da tela de Inconsistências
    
    root.mainloop()
